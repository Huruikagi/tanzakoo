# Tanzakoo 開発ルール

## プロダクトの方針

- 作業前に `notes/product-idea.md` の合意済み方針を確認する。検討履歴や未決の案を確定仕様として扱わない。

## 依存関係の採用

- 必要に応じて、既存のライブラリ・フレームワーク・SDK・アダプターを積極的に採用する。
- 汎用的な機能を自作し始める前に、既存の依存関係や利用可能な実装で満たせないか確認する。車輪の再発明を避ける。
- 新しい依存関係を追加する前に、ユーザーへ名前・用途・採用理由を短く伝える。例：「ドラッグ操作には○○を使います。キーボード操作も含めた既存実装を利用するためです。」
- 通常の依存追加は、この事前説明を行ったうえで進めてよい。毎回の承認待ちは不要。無言で追加しない。
- 料金・外部サービスへのデータ送信・既存方針の変更など、新たな判断が必要になる場合は、その具体的な選択をユーザーに確認する。
- 候補の用途への適合性、保守状況、ライセンス、採用する環境・バージョンとの互換性を確認する。採用そのものを目的に依存を増やさない。

## 開発ツール

- Node.jsとpnpmはプロジェクトの `mise.toml` でバージョンを管理する。実行時はmiseで選択したものを使う。
- `package.json` を作成するときは `packageManager` をmiseのpnpmバージョンと揃え、バージョン更新時も同期する。
- JavaScript / TypeScriptのlint・整形にはユーザー指定のBiomeまたはOxlint / Oxfmtを使う。現在の第一候補は、`@shadcn/lint` と組み合わせるOxlint + Oxfmt。ESLint + Prettierを初期設定として追加しない。
- `@shadcn/lint` をOxlintのJSプラグインとして導入済み。更新時はOxlint・TypeScriptパーサーとの互換性を確認する。可変ペインやドラッグ操作に必要な動的スタイルを一律禁止しない。

## GitHub URL handling

- GitHub URL が含まれる依頼では、利用可能な GitHub 連携を優先して、リポジトリ、Issue、PR、Commit、Discussion の情報を取得する。
- 連携で不足する情報がある場合は、認証済みの `gh` を使用する。
- `gh` が未認証または権限不足の場合は、その旨を報告して `gh auth login` を案内する。

## 実行権限

- 子プロセスの起動、依存関係のダウンロード、外部プロセスの開始などでサンドボックス制限による失敗が予見できる場合、通常実行を試さない。
- 最初から `require_escalated` を指定し、実行目的を日本語で簡潔に説明して承認を求める。
